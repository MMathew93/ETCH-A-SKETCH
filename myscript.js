const container= document.querySelector('#container');

const row= document.querySelector('div');

const column= document.querySelector('div');

const cell= documents.querySelector('div');

//creates a default grid sized 16x16
function grid() {
    row(16);
    column(16;)
};


//functions to change row and column
function gridSize(row, column) {
};

function rowSize() {
    for(let i= 0; i<row; i++) {
        //update row in div
    }
};

function columnSize() {
    for(let i=0; i<column; i++) {
        //update column in div
    }
};